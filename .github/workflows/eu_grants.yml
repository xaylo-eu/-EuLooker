name: EU Grants Agent

on:
  schedule:
    - cron: "30 6 * * 1"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  run-agent:
    runs-on: ubuntu-latest
    timeout-minutes: 60

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install requests

      - name: Run EU Grants Agent
        run: python eu_grants_agent.py
        env:
          EMAIL_HESLO: ${{ secrets.EMAIL_HESLO }}

      - name: Save seen identifiers
        run: |
          git config user.email "actions@github.com"
          git config user.name "GitHub Actions"
          git add seen_identifiers.json users.json
          git commit -m "update data [skip ci]" || echo "nothing to commit"
          git push
